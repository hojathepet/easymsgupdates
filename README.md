# EasyMsg Updates

Public **binary-only** update channel for EasyMsg.

- `latest.json`: current version, download URL, and SHA-256
- `EasyMsg-Windows-x64.zip`: current Windows x64 package
- No EasyMsg source code is published here.
- Do not store tokens, private keys, or other secrets in this repository.

The private source repository remains separate.
